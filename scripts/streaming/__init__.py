"""Per-shard streaming operational package.

Sibling to the production ``osm_polygon_sentence_relevance.application``
modules. Imported as ``scripts.streaming.*`` in operational scripts and
in the unit-test suite.

This operational package is shipped in the source distribution via
``MANIFEST.in`` and deliberately excluded from the importable wheel.
"""
